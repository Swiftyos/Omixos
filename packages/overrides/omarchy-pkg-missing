#!/bin/bash

set -euo pipefail

# Nothing can be missing from an empty list, matching upstream's semantics.
(( $# > 0 )) || exit 1

for package in "$@"; do
  if ! omarchy-pkg-present "$package"; then
    exit 0
  fi
done

exit 1
