#!/bin/bash

# omarchy:summary=Print the OmixOS quattro port version and upstream revision

revision="$(cat "${OMARCHY_PATH:-/usr/share/omarchy}/UPSTREAM_REVISION" 2>/dev/null || true)"
echo "quattro-nixos (${revision:-unknown})"
